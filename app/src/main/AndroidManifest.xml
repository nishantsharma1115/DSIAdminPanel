<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.dsiadminpanel">

    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".employeeProfileActivity" />
        <activity android:name=".customerListActivity" />
        <activity android:name=".navPanchayatListActivity" />
        <activity android:name=".districtCoordinatorListActivity" />
        <activity android:name=".distributorListActivity" />
        <activity android:name=".zoneCoordinatorListActivity" />
        <activity android:name=".employeeDashboardActivity" />
        <activity android:name=".blockCoordinatorListActivity" />
        <activity android:name=".stateCoordinatorListActivity" />
        <activity android:name=".generateEmployeeActivity" />
        <activity android:name=".pendingEmployeeProfileActivity" />
        <activity android:name=".pendingFormListActivity" />
        <activity android:name=".adminDashboardActivity" />
        <activity android:name=".loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".employeeRegistrationFormActivity" />
    </application>

</manifest>